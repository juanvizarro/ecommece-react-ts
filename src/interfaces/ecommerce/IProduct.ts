export interface IProduct {
    id: number
    thumbnail: string
    title: string
    description: string
    price: number
    stock: number
    discountPercentage: number
    rating: number
    brand: string
    category: string
    images: string[]
}